---
name: shuati
description: 中级经济师刷题。用户说刷题、做题、出题、考我、错题复习、某个考点讲讲，或输入 /shuati 时用。带权重复习机制和自动错题本。
---

# 中级经济师刷题

题库两门课**完全分开**，不要混着出题：

- **人力资源** = 《人力资源管理专业知识和实务》，330 考点 / 3655 题
- **经济基础** = 《经济基础知识》，582 考点 / 4562 题

> **判卷只认题目ID。** 抽题时记下 `# 题号→ID` 那一行，判卷时用 `answers` 按ID回查。
> **任何情况下都不要拿题干关键词去题库里反查答案**——题库里「题干几乎一样、答案不一样」
> 的重题有 166 组（同考点的单选/多选版、不同年份的改编版），关键词匹配必然串题。

同一道真题常在「章节真题」和「整卷」里各收一份，取题时默认自动去重（8217 题里可刷 8034，
去重后 7562）。想连着重复的一起刷加 `--保留重题`。

资料按 2025 年考纲整理，2026 教材章号会变，所以题库按**主题模块**组织，章号只是次级标签。
考点的 ★ 和「近 4 年考察次数」来自考频统计，是排优先级的依据。

## 先定位题库

每次开工先跑一次，拿到路径和现状：

```bash
for d in "$SHUATI_HOME" ~/.claude/skills/zhongjing-shuati ~/zhongjing-shuati \
         /sessions/*/mnt/*/zhongjing-shuati ./zhongjing-shuati .; do
  [ -f "$d/scripts/pick.py" ] && P="python3 $d/scripts/pick.py" && break
done
echo "$P" && $P overview
```

`pick.py` 自己会找数据：先看 `$SHUATI_BANK`，再看脚本旁的 `data/`，再看上一级。
报「找不到题库」说明 `data/` 是空的——仓库默认不带题库数据，让用户把数据放进
`<仓库>/data/`（里面是 `人力资源/`、`经济基础/` 两个目录），或者设 `SHUATI_BANK` 指过去。
题库只读时用 `SHUATI_HOME` 指定错题本写在哪。

overview 会打印两科的模块、每个题目文件的题数/可刷数/题型分布，以及错题本进度。

## 取题命令

```bash
# 抽题（科目必填，其余可选）。默认加 --藏答案：只出题面，答案留到判卷时按ID取
$P pick --科目 人力 --n 10 --藏答案
$P pick --科目 人力 --范围 组织行为 --n 10       # 限定模块（也接受章名、题源类型）
$P pick --科目 基础 --星级 3 --n 15             # 只出 ★★★ 高频考点
$P pick --科目 人力 --真题 --n 20               # 只出历年真题
$P pick --科目 基础 --题型 多选题 --n 10
$P pick --科目 人力 --考点 需要层次理论 --n 5
$P pick --科目 人力 --n 10 --排除已做            # 完全跳过做过的（少用，见下）
$P pick --科目 人力 --n 10 --均匀                # 关掉权重，纯随机
$P pick --科目 人力 --n 10 --保留重题            # 不去重（默认去重）

# 判卷：按题目ID取答案+解析，带上用户作答就直接判对错并算分
$P answers HR-Q00123:AB EB-Q00353:C ...
$P answers HR-Q00123 EB-Q00353                  # 只取答案不判
$P answers HR-Q00123:AB --全选项                 # 连选项原文一起打出来

# 考点讲解：考点信息 + ★ + 考过哪些年 + 关联真题
$P kp 需要层次理论 --科目 人力 --n 3

# 错题
$P wrong --科目 人力 --n 10 --藏答案             # 取错题重做（到期的排前面）
$P weak  --科目 人力                            # 按错题本算薄弱考点
$P logs HR-Q00123:错:AB HR-Q00456:对:C ...      # 批量记，判完一批用这个
$P log --题目ID HR-Q00123 --结果 错 --我的答案 AB   # 记单条
```

`--范围` 是模糊匹配，`模块3`、`试卷_机考真题`、`第7章`、`模考预测卷`、节名都能当范围传。

`--n` 统计顶层题：普通题 1 道算 1，完整案例组也算 1，不会按子题数截断案例。
`# 题号→ID` 里案例组会写成 `1=HR-Q001,HR-Q002` 这种形式；判卷、logs、错题权重和
知识点复习仍按每个子题 ID 分别记录。`wrong` 遇到错过的案例子题时，会重出整个原案例组；
`--排除已做` 只有在案例组所有子题都做过时才跳过该案例。

`pick` / `wrong` 输出开头有两行注释：`# 题号→ID` 是判卷的唯一凭据，
`# 判卷：` 是拼好的 `answers` 命令模板，把 `用户答案` 换成用户实际选的字母就能跑。
`answers` 末尾还会打印拼好的 `logs` 命令，照抄即可。

## 权重机制（默认开着）

抽题不是均匀随机，像背单词软件那样按权重抽：**答对的逐步少见，答错的变常见。**

权重不存表，`pick.py` 每次从做题流水现算——连续答对升一级、间隔翻倍、权重减半；
**答错直接打回第 0 级并立刻到期**，而且「错过几次」长期抬高权重，攻克了也比从没错过的勤。
间隔是 0/1/2/4/8/16/32/64 天。实际倍数（拿全新题当 1.0 基准）：

| 情形 | 权重 |
|---|---|
| 全新没做过 | 1.0 |
| 今天刚答对 | 0.03（当天基本不会再出） |
| 昨天答对 1 次 | 0.5 |
| 连对 3 次 | 0.07 |
| 连对 5 次 | 0.01（基本退休） |
| 今天答错 1 次 | 1.8 |
| 累计错 2 次 | 2.6 |
| 错过 1 次后又连对 3 次 | 0.17（比纯没错过的 0.07 高，留个尾巴） |

- 别拿 `--排除已做` 当默认。做过的题本来就会自动少出现，`--排除已做` 是彻底不再出，
  等于把复习也掐掉。只有用户明确说「只要没做过的」才加。
- `pick` 输出每题带状态标签（`新题`、`L2·3天前`、`L0·错2次·今天做过`）。
  这是给你看的，**贴给用户时不要带**。
- 用户问「怎么又是这道题」→ 上次答错了，权重被抬高，这是设计好的。

## 错题本

`_错题本.md`（默认写在题库根目录，可用 `SHUATI_HOME` 改），用户直接打开看的东西。
**用途是记住正确知识，不是重做题。**

每次 `log`/`logs` 由 pick.py 整篇重写：按科目 → 考点分组（错得多的考点排前面），
每题带完整题干选项、✅标正确答案、`←我错选了`标当时踩的坑、**「要记住的知识点」**
（本题解析 + 同一考点其他题的解析归并成完整那张清单）、错过几次、下次复习在几天后。
后来做对的挪到「已攻克」。

文件末尾 `## 做题流水` 里的 tsv 代码块是程序读的事实来源，**不要手改，也不要自己往 md 里写题**——
一律走 `logs`，正文会被覆盖。

## 出题规矩

用户没说科目就先问（人力资源 / 经济基础 / 两科混着），顺便问范围和题量。
说了就直接开始，别再确认。

**流程固定四步，全程靠题目ID串起来：**

1. `$P pick ... --藏答案` 抽题。**先把 `# 题号→ID` 那行原样留在你的工作笔记里**
   （贴给用户时不要带）。案例组的一个题号会对应多个子题 ID。
2. 一次把所有题贴出来，编号 1..N，每题只给题型、题干、选项。案例先给完整材料，
   子题按 `1.1 / 1.2` 列出，等用户逐个小题作答。
3. 用户答完，`$P answers <ID>:<他选的> ...` ——ID 按第 1 步那行取，顺序和题号一致；
   案例的每个子题都要单独传 ID 和作答。
   对错、漏选多选、得分都是脚本算的，你不要自己比字母。
4. `$P logs ...` 记账（`answers` 末尾已经把命令拼好了）。

`--藏答案` 就是为了让你手里根本没有答案，想泄也泄不出去。真要一次拿全（比如用户要
「出题连答案一起给我打印」），去掉 `--藏答案`，那份输出里的答案和解析**一个字都不许提前泄**。

**贴题面时扣着不给的**：答案、解析、考点名、年份、权重标签（考点名会直接漏答案）。
贴完就等，别催。

用户分批回答，一条消息里可能只答 3 道也可能答 10 道，顺序也可能跳：

1. **他回几道你就判几道**，没回的不替他答、不催、不重贴题面。
2. 认各种写法：`b`、`B`、`bd`、`B、D`、`选BD`、`3.C`、`1B 2ABE 3C`、一行一个都算。
   带题号的按题号对；不带题号的按顺序补位（从第一道还没判的开始）。
   认不出来是哪道题就问一句，别猜。
3. 判完这批的 `answers` 结果落地成回复：对/错 → 正确答案 → 解析（用自己的话捋顺，
   别贴原样的断行乱码）→ 考点名 + ★ + 考过的年份。错的必须补一句**他选的那个为什么不对**
   （`answers` 会把争议选项的原文一起打出来，照着讲）。
4. 判完这批立刻 `logs` 记一次（一批一次调用，不要一题调一次）。对的错的都要记，
   权重和错题本都靠它。
5. 全部答完再给小结：对错数、错在哪几个考点、下一步练什么范围。

多选题按整题算对错。中级经济师规则是**多选、错选、不选都 0 分；少选每个选中的得 0.5 分**，
`answers` 已经按这个算好分了，直接引用它给的数，别自己重算。

案例分析题会连着材料一起抽出来（同一组的子题不拆），一个案例算一道顶层题；
贴的时候材料放前面，子题一道道列。
章节真题里的「案例题」是从真题案例里拆出来挂到考点下的单题，原文就没有材料，
照普通单选/多选出就行，别说「材料呢」。
错题重做同样整组展开，但判卷和记账仍按子题 ID 走，知识点和权重记忆不会合并或丢失。

## 不要做的事

- **不许用题干关键词反查答案。** 判卷、复盘、用户问「刚才第6题答案是啥」，一律 `answers <ID>`。
  题库里题干归一化后完全相同、答案却不同的重题有 166 组——「借贷记账法的记账规则**是**」
  答 A，「记账规则**不包括**」答 DE；「决定劳动供给曲线后弯的因素」单选版答 C、多选版答 AC、
  另一版答 CE。靠题干挑，挑中的大概率是隔壁那道。ID 找不到就说找不到，**不要猜、不要换成
  相似题的答案**。
- **不出 `质量: 仅参考` 的题**。pick 已经过滤掉了（183 道：OCR 把某个选项行整行丢了，
  还有 11 道是两处收录给的答案自己打架），别绕过 pick 直接读 jsonl 出题。
- 不要凭自己的知识改题库里的答案。用户质疑答案时，报 `题源类型` + `章节` + `源页码`
  （`answers` 的「出处」行就是），让他对着原始资料核；确实是 OCR 错了就告诉他记一笔，
  别偷偷改库。
- 不要跨科目混编，除非用户明确要「两科混着」。
- 不要一道一道地问。要的题一次全贴出来，然后按他回的批次判——见「出题规矩」。
- 不要在他还没答完的时候先讲答案、先给提示、先说「这题容易错」。

## 数据长什么样

`data/<科目>/`：

- `知识点.jsonl` — 考点骨架：知识点ID、★考频星级、近4年频次、考察年份、模块、章节、所属节、题数
- `章节.jsonl` — 每章近 3 年平均分值、是否考案例题、学习建议
- `考点总览.md` — 人看的目录，按模块 → 章 → ★ 排序
- `题目/模块N_XX.jsonl`、`题目/试卷_XX.jsonl`、`题目/专项_XX.jsonl`

`data/_存疑清单.md` 是那 183 道残缺题的清单，带章节和页码。

题目字段：题目ID、知识点ID、知识点、挂靠依据、模块、章节、题源类型、考试年份、资料版本、
题型、题干、选项、答案、解析、案例组、案例材料、源页码、存疑、质量、重题组。

`挂靠依据` 说明这题的考点是怎么定的：`位置直接`（原文就标在这个考点下，最可靠）、
`本题考查·精确/包含/相似`（解析里写了「本题考查XX」）、`文本包含`、`未归点`。
讲解时如果依据是「相似」，措辞留点余地。1549 道未归点的题照样能刷，只是没绑考点。
